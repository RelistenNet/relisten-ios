language: swift
xcode_workspace: Relisten.xcworkspace
xcode_scheme: Relisten
os: osx
osx_image: xcode12.2
xcode_scheme: Relisten
install:
    - ./setup.sh
script:
    - ./xcodebuild.sh
git:
  depth: 3
rvm: 2.6.6
cache:
    - bundler
